<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link type="text/css" href="/css/style.css" rel="stylesheet">
    <title>〇×ゲーム</title>
</head>
<body>
    <%- include('header'); %>

    <form action="/adduser" method="post">
        <div class="form-container">
            <p class="form-heading">名前</p>
            <input type="text" name="userName" class="input-text">
            <br>
        </div>
        <div class="form-container">
            <p class="form-heading">色</p>
            <label for="" class="selectbox">
                <select name="userColor">
                <% for(i=0;i<colors.length;i++){ %>
                    <option><%= colors[i].color %></option>
                <% } %>
                </select>
            </label>
            <br>
        </div>
        
        <input type="submit" value="登録" class="inputbtn">
    </form>

    <%- include('footer'); %>
</body>
</html>